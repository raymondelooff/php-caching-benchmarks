bench:
  image: raymondelooff/php-caching-benchmarks
  environment:
    - REDIS_HOST=redis.docker
    - REDIS_PORT=6379
redis:
  image: "redis:alpine"
  networks:
        data:
          aliases:
            - redis.docker
